import { TooglerDirective } from './toogler.directive';

describe('TooglerDirective', () => {
  it('should create an instance', () => {
    const directive = new TooglerDirective();
    expect(directive).toBeTruthy();
  });
});
